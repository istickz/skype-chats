%nav.navbar.navbar-default.navbar-fixed-top{role: "navigation"}
  .container
    .navbar-header
      %button.navbar-toggle{"data-target" => ".navbar-ex1-collapse", "data-toggle" => "collapse", type: "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to 'SkypeChats', root_path, class: "navbar-brand"
    / Collect the nav links, forms, and other content for toggling
    .collapse.navbar-collapse.navbar-right.navbar-ex1-collapse
      %ul.nav.navbar-nav
        - if user_signed_in?
          %li
            = link_to(new_chat_path ) do
              %i.fa.fa-plus-circle>
              Добавить чат
        %li
          = link_to "Чаты", chats_path
        %li
          = link_to "Помощь", help_path
        %li
          - if user_signed_in?
            %a.dropdown-toggle{"data-toggle" => "dropdown", href: "#"}
              = current_user.nickname
              %span.caret
            %ul.dropdown-menu
              %li
                = link_to "Профиль", profile_path
              %li
                = link_to "Настройки", edit_user_registration_path
              %li
                = link_to "Выход", destroy_user_session_path, method: :delete
          - else
            = link_to(user_omniauth_authorize_path(:twitter)) do
              %i.fa.fa-twitter.fa-fw
              %span.network-name Вход
